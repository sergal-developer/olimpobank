import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.olimpobank.mx.app',
  appName: 'olimpobank',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
