import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.olimpoapp.mx.app',
  appName: 'olimpoapp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
